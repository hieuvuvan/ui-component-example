import { RouteObject } from "react-router-dom";
import { IndexTableExample } from "./IndexTableExample";

export const routes: RouteObject[] = [
    {
        path: "table",
        Component: IndexTableExample
    }
]